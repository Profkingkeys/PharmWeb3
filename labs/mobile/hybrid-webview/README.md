# Hybrid WebView Lab

Minimal Android/Kotlin/Java project demonstrating a WebView with a JavaScript-to-native telemetry bridge.

## Build

Open this directory in Android Studio.

GitHub Actions compiles the debug APK. The current coding terminal does not contain the Android SDK/Gradle toolchain, so I do not claim a local Android build.
